import Form from "@rjsf/core";

import React from 'react'
const schema = require("../Schema.json");

const File = () => {
  return (
    <Form schema={schema}>
        
        </Form>
  )
}

export default File