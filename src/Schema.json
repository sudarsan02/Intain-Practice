{
  "title": "",
  "description": "Create Pool",
  "type": "object",
  "required": [
    "Pool Name",
    "Pool Description",
    "Pool Owner Name"
  ],
  "properties": {
    "Pool Name": {
      "type": "string",
      "placeholder": "Pool Name"
    },
    "Pool Description": {
      "type": "string",
      "placeholder": "Pool Description"
    },
    "Pool Owner Name": {
      "type": "string",
      "placeholder": "Pool Owner Name"
    },
    "Pool start Date": { 
  "type": "string", 
  "format": "date-time"
},
"Pool Expiry Date": { 
  "type": "string", 
  "format": "date-time"
}
  }
}